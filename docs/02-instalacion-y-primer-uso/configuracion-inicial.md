# 2.3 Configuración inicial

Copiar `ada/config.example.json` a `ada/config.json`, revisar `allowed_roots`, `ollama_url`, `ollama_backend` y los límites de memoria. Las listas vacías de seguridad son fail-closed.
