# 2.1 Requisitos

- Python compatible con el proyecto.
- Ollama instalado y ejecutándose para modelos locales.
- Git.
- Dependencias del proyecto y, opcionalmente, los extras `litellm`, `web`, `credentials` o `vision`.
