# 2.2 Instalación

```bash
git clone https://github.com/FedeMarkoo/ADA-IA.git
cd ADA-IA
python -m venv .venv
source .venv/bin/activate
pip install -e '.[dev,litellm]'
```

En Windows, activar el entorno con `.venv\\Scripts\\activate`.
