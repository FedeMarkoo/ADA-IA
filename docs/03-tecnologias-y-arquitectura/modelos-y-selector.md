# 3.6 Modelos y selector

El router aporta `task_type`, `complexity`, privacidad y `model_hint`. El selector resuelve esas señales contra un catálogo allowlisted, considerando hardware, latencia, errores y fallbacks.

![Modelos de ADA](assets/modelos.png)
