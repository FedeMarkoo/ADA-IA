# 3.4 Persistencia y modelo de datos

SQLite almacena conversaciones, resúmenes, memorias, tareas, procedimientos, auditoría y eventos. El WAL y las conexiones por thread permiten lecturas concurrentes; el cifrado Fernet protege columnas sensibles cuando está habilitado.
