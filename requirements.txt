Flask>=2.3,<4
numpy>=1.24,<3
Pillow>=9,<12
psutil>=5.9,<8
rawpy>=0.27,<0.28

# Optional capabilities:
# pip install -e '.[vision,gpt4all,web-search,remote-llm]'

# Optional remote LLM backends. Ollama itself is installed separately as a system app.
